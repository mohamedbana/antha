name: antha
peanutversion: 0.0.1
version: 0.0.2
dependson:
modules:
    golang:
        package: github.com/antha-lang/antha
        buildpaths:
            - ~internal
    docker:
        packages:
            - libglpk-dev
            - libsqlite3-dev
